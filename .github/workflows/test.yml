name: Test

on:
  workflow_call: # Triggered manually or by other workflows

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Validate HTML files
        run: echo 'No tests available for static site.'
